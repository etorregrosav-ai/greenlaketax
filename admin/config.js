// Green Lake — configuración de conexión a Supabase.
// Sustituye estos dos valores por los de tu proyecto:
// Supabase → Project Settings → API → Project URL / anon public key.
// Es seguro que estos dos valores sean públicos: la protección real
// la da Row Level Security (RLS) en la base de datos, no ocultar esto.

const SUPABASE_URL = "PENDIENTE_PROJECT_URL";
const SUPABASE_ANON_KEY = "PENDIENTE_ANON_KEY";
